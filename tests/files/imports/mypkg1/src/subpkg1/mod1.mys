from .. import var2

def func1(first: i32, second: i32) -> i32:
    return first + second

def func2(first: i32, second: i32) -> i32:
    return first * second

class Class1:

    value: i32 = 5
        
class Class2:
    pass

@test
def test_func1():
    assert_eq(add(1, 2), 3)

@test
def test_var2():
    assert_eq(var2, 7)
