COMPARE_MANY_1_FOO: i64 = 0

def compare_many_1_foo() -> i64:
    COMPARE_MANY_1_FOO += 1

    return 2

@test
def test_compare_many_1():
    four = 4

    # if (!([&]() {
    #           const auto &v_1 = compare_many_1_foo();
    #           if (!(1 < v_1)) {
    #               return false;
    #           }
    #           const auto &v_2 = 3;
    #           if (!(v_1 < v_2)) {
    #               return false;
    #           }
    #           if (!(v_2 <= four)) {
    #               return false;
    #           }
    #           return true;
    #       }()))
    if not (1 < compare_many_1_foo() < 3 <= four):
        assert False

    assert COMPARE_MANY_1_FOO == 1
    
@test
def test_compare_many_2():
    if 1 < 2 in [1, 3]:
        assert False

