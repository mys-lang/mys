def from_return_type() -> ([string], {i64}, {char: bool}):
    # ToDo: Remove types.
    x: [string] = []
    y: {i64} = {}
    z: {char: bool} = {}

    return x, y, z

@test
def test_from_return_type():
    assert from_return_type() == ([], {}, {})
