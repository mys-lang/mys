@test
def test_i8():
    x: i8 = 10

    assert_eq(x, 10)

@test
def test_i16():
    x: i16 = 10

    assert_eq(x, 10)

@test
def test_i32():
    x: i32 = 10
    y: i32 = -2

    res: i32 = (5 + 2 * x) - 8 / y
    assert_eq(res, 29)

    res = ((5 + 2 * x) - 8) / y
    assert_eq(res, -8)

    res = 3 ** 2
    assert_eq(res, 9)

    res = 1 << 2
    assert_eq(res, 4)

    res = 4 >> 2
    assert_eq(res, 1)

    res = 1 | 8
    assert_eq(res, 9)

    res = 1 ^ 3
    assert_eq(res, 2)

    res = 1 & 3
    assert_eq(res, 1)

    res = 10 % 4
    assert_eq(res, 2)

@test
def test_i64():
    x: i64 = 10

    assert_eq(x, 10)

@test
def test_u8():
    x: u8 = 10

    assert_eq(x, 10)

@test
def test_u16():
    x: u16 = 10

    assert_eq(x, 10)

@test
def test_u32():
    x: u32 = 10

    assert_eq(x, 10)

@test
def test_u64():
    x: u64 = 10

    assert_eq(x, 10)
