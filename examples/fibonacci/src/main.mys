def fibonacci(number: int) -> int:
    if number <= 1:
        return number
    else:
        return fibonacci(number - 1) + fibonacci(number - 2)

def main():
    for i in range(11):
        print(i, "=>", fibonacci(i))
