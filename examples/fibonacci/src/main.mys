def fibonacci(n: int) -> int:
    if n <= 1:
        return n
    else:
        return fibonacci(n - 1) + fibonacci(n - 2)


def main():
    for i in range(11):
        print(fibonacci(i))

    a: int = 5
    b: int = 127
    c: int = a + b

    print(c)
    print(a + b)
    print(a + 127)
