def fibonacci(n: int) -> int:
    if n <= 1:
        return n
    else:
        return fibonacci(n - 1) + fibonacci(n - 2)


def main():
    for i, j in enumerate(range(11)):
        print(i, "=>", fibonacci(j))
