name: Test in Cygwin

on:
  schedule:
    - cron:  '15 4 * * *'
  workflow_dispatch:

jobs:

  windows:

    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v1
    - name: Test
      run: |
        ./.github/workflows/cygwin.bat
